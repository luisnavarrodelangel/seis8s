module InstrumentState where

--this is about what do I need to keep track of ? -- better to keep it empty for now

data InstrumentState = InstrumentState deriving (Show)

emptyInstrumentState = InstrumentState
